package com.cercli.repository;

public class EmployeeRepositoryTest {
    // Functional tests (with full setup)
    // to verify that models are saved and returned from a db with the same data
    // to verify that findById returns a right employee for the provided id
    // to verify that findFor returns employees for the provided country id and zone id
    //      or only country id if zone id is null
    // to verify that getAll returns all employees for the db
}
