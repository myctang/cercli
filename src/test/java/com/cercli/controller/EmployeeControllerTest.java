package com.cercli.controller;

public class EmployeeControllerTest {
    // Functional tests (with full setup)
    // to verify that addEmployee endpoint stores a model in the db
    // to verify that updateSalary endpoint updates salary and updated at in the db
    //      returns 404 in case the provided id is incorrect
    // to verify that findById endpoint returns a model for the provided id from the db
    //      returns 404 in case the provided id is incorrect
    // to verify that getAll returns all models from the db
}
